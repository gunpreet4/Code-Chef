#include<iostream>
using namespace std;
int main()
{
    int t;
    long int a,b,c,d,x,y;
    while(t--)
    {
        cin>>a>>b>>c>>d;
       x= >=a && <=b;
       y= >=c && <= d
       cout<<x<<endl;
    }
    return 0;
}
